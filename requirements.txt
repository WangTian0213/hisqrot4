# HiSQRot4 runtime dependencies.
#
# Install the pinned PyTorch stack separately before this file:
#
#   pip install \
#     torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0 \
#     --index-url https://download.pytorch.org/whl/cu128
#
# The local reference environment used:
#   Python 3.10.20
#   torch==2.10.0+cu128
#   torchvision==0.25.0+cu128
#   torchaudio==2.10.0+cu128
#   triton==3.6.0
#
# CUDA/ABI-sensitive packages such as detectron2 are documented in README.md
# and should be installed after PyTorch. The flash-attn entry below uses a
# prebuilt wheel for Linux x86_64, Python 3.10, torch 2.10.0+cu128.
#
# VBench is intentionally not included here because vbench==0.1.5 pins
# transformers==4.33.2, while Wan2.2 inference uses transformers==4.51.3.
# See requirements-vbench.txt and README.md for the optional evaluation setup.

transformers==4.51.3
diffusers==0.37.0
tokenizers==0.21.4
accelerate==1.13.0
peft==0.18.1
safetensors==0.7.0
huggingface-hub[cli]

numpy==1.26.4
opencv-python==4.11.0.86
decord==0.6.0
librosa==0.11.0
imageio==2.37.3
imageio-ffmpeg==0.6.0
einops==0.8.2
tqdm==4.67.3
psutil==7.1.3
ninja==1.13.0
flash-attn @ https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.9.0/flash_attn-2.8.3%2Bcu128torch2.10-cp310-cp310-linux_x86_64.whl

easydict==1.13
ftfy==6.3.1
dashscope==1.25.14
